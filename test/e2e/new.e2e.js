describe('new state', function () {
  it('should list available repositories', function () {
  });

  it('should add repositories', function () {
  });

  it('should list seed dockerfiles', function () {
  });

  it('should select seed dockerfile', function () {
  });

  it('should edit of dockerfile', function () {
  });

  it('should build', function () {
  });

  it('should not allow duplicate project names', function () {
  });
});
