describe('build state', function () {
  it('should show build meta data', function () {
  });

  it('should not display run instance button if build not successful', function () {
  });

  it('should display run instance button only if build successful', function () {
  });

  it('should have edit button', function () {
  });

  it('should not allow edit', function () {
  });

  it('should have correct return-to-build-list button', function () {
  });

  it('should allow rebuild', function () {
  });

  it('should open files', function () {
  });

  it('should close files', function () {
  });

  it('should toggle selected file', function () {
  });


});
