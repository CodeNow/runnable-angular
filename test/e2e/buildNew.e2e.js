xdescribe('buildNew state', function () {
  it('should allow edit', function () {
  });

  it('should allow build', function () {
  });

  it('should display error and not allow build if invalid dockerfile', function () {
  });

  it('should open files', function () {
  });

  it('should close files', function () {
  });

  it('should toggle selected file', function () {
  });

  it('should list repositories', function () {
  });

  it('should delete repositories', function () {
  });

  it('should modify repositories', function () {
  });

});
