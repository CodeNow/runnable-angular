'use strict';

var Base = require('../base');
var util = require('util');
module.exports = Environments;

function Environments () {
  Base.apply(this, arguments);
}

util.inherits(Environments, Base);

Environments.prototype.urlPath = 'environments';

Environments.prototype.Model = require('../../models/project/environment');
