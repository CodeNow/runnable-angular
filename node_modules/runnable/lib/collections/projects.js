'use strict';

var Base = require('./base');
var util = require('util');
module.exports = Projects;

function Projects () {
  Base.apply(this, arguments);
}

util.inherits(Projects, Base);

Projects.prototype.urlPath = 'projects';

Projects.prototype.Model = require('../models/project');
