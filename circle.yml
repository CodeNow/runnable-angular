dependencies:
  override:
    - npm install grunt -g
    - npm install jade -g
    - npm install karma -g
    - npm install
    - grunt jade2js
    - grunt jshint:prod
    - grunt autoBundleDependencies
    - grunt browserify
    - karma start ./test/karma.conf.circleci.js
machine:
  node:
    version: v0.10.25