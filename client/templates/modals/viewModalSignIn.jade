.modal-backdrop(
  ng-class = "{in: in}"
)
  .modal-dialog.modal-sign-in(
    ng-click = "$event.stopPropagation()"
  )
    //-
    //- if prompted to sign in to fork:
    //-
    //- header.modal-header
    //-   h1.modal-heading Sign In to Fork
    //- section.modal-body
    //-   p You have to sign in to fork this box.
    //- footer.modal-footer
    //-   a.btn.green
    //-     svg.iconnables.icons-git
    //-       use(
    //-         xlink:href = "#icons-git"
    //-       )
    //-     | Sign In with Github
    //-   a.link.link-text.link-secondary I&rsquo;m not ready yet.

    //-
    //- if prompted to sign in to add repos:
    //-
    //- header.modal-header
    //-   h1.modal-heading Sign In to Add Repos
    //- section.modal-body
    //-   p You have to sign in to Github to add your repositories to this box.
    //- footer.modal-footer
    //-   a.btn.green
    //-     svg.iconnables.icons-git
    //-       use(
    //-         xlink:href = "#icons-git"
    //-       )
    //-     | Sign In with Github
    //-   a.link.link-text.link-secondary I&rsquo;m not ready yet.

    //-
    //- after signing in:
    //-
    header.modal-header
      h1.modal-heading You&rsquo;re In!
    section.modal-body
      p Now that you're signed in, we can help you
        br
      | get your own projects running.
    footer.modal-footer
      a.btn.green(
        ng-click = "actions.nextPage()"
      ) Start Building My Project
      a.link.link-text.link-secondary I don't want any help