//- instance list
.grid-block.shrink.vertical.list-instances(
  ng-class = "{\
    'deprecated': !$root.featureFlags.composeNav,\
    'noscroll': CA.shouldShowTeamCTA()\
  }"
  ng-if = "CIS.instancesByPod"
  ng-include = "'viewInstancesList'"
  scroll-offset = "200"
  scroll-to = "a.a-sref.active"
)

.grid-block.shrink.vertical.list-instances(
  ng-if = "!CIS.instancesByPod"
)
  .spinner-wrapper.spinner-md.spinner-gray.spinner-center.in(
    ng-include = "'spinner'"
  )

.grid-block.noscroll.vertical.instance-wrapper(
  ui-view
)
