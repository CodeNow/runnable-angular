#editor-container
  pre(
    ui-ace
    ng-model = "openFiles.activeFile.state.body"
    readonly = "{{ readOnly }}"
    ng-class = "{ \
      readonly: readOnly, \
      'ace-runnable-dark': isDarkTheme \
    }"
  )
