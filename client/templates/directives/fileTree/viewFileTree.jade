ul.file-tree(
  popover-file-explorer-menu
)
  //- this is the root dir; there can be only one.
  file-tree-dir(
    open = "true"
    dir = "data.rootDir"
    versionOrContext = "versionOrContext"
    open-files = "openFiles"
    is-clean = "isClean"
  )
