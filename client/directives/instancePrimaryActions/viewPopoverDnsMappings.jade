.popover.bottom.popover-dns-mappings(
  ng-class = "{'in': active}"
  ng-init = "\
    dnsMappings = [\
      {name: 'data-dog', elasticHostname: 'datadog-staging-CodeNow.runnableapp.com', branch: 'master', sandbox: 'Private Sandbox (runnabro)'},\
      {name: 'mavis', elasticHostname: 'mavis-staging-CodeNow.runnableapp.com', branch: 'master', sandbox: 'Private Sandbox (runnabro)'},\
      {name: 'neo4j', elasticHostname: 'neo4j-staging-CodeNow.runnableapp.com', branch: 'master', sandbox: 'Private Sandbox (runnabro)'},\
      {name: 'optimus', elasticHostname: 'optimus-staging-CodeNow.runnableapp.com', branch: 'master', sandbox: 'Private Sandbox (runnabro)'},\
      {name: 'runnable-angular', elasticHostname: 'runnable-angular-staging-CodeNow.runnableapp.com', branch: 'master', sandbox: 'Private Sandbox (runnabro)'}\
    ];\
    mavisBranches = [\
      'master',\
      'abort-on-uncaught-exception',\
      'bypass-prev-dock',\
      'no-docks-404',\
      'remove-numBuild',\
      'temporary-branch-to-fork-on-runnable'\
    ];\
    state.view = 1;\
  "
  ng-style = "popoverStyle.getStyle()"
  style = "transform-origin: 50% 0"
)
  .arrow
  .popover-views
    //- min-height is height of list items + header + list padding
    div(
      ng-class = "{\
        'view-1': state.view === 1,\
        'view-2': state.view === 2\
      }"
      style = "min-height: {{state.view === 1 ? dnsMappings.length*54 + 36 + 8: mavisBranches.length*36 + 36 + 8}}px"
    )

      .popover-view
        .popover-header
          .span(
            ng-style = "state.view === 1 && {'opacity': '1'}"
          ) DNS Mappings

        ul.list.popover-list(
          ng-style = "state.view === 1 && {'opacity': '1'}"
        )
          li.list-item.popover-list-item.tr.multi-line.clearfix(
            ng-click = "state.view = 2"
            ng-repeat = "container in dnsMappings"
          )
            .cell.text-overflow {{container.name}}
              small.small.text-overflow(
                title = "{{container.elasticHostname}}"
              ) {{container.elasticHostname}}
            .cell.text-overflow {{container.branch}}
              small.small.text-overflow {{container.sandbox}}
            svg.iconnables.icons-arrow-forward.float-right
              use(
                xlink:href = "#icons-arrow-down"
              )

      .popover-view
        .popover-header
          svg.btn.btn-sm.iconnables.icons-arrow-backward.pull-left(
            ng-click = "state.view = 1"
            ng-style = "state.view === 2 && {'opacity': '1'}"
          )
            use(
              xlink:href = "#icons-arrow-down"
            )
          .span(
            ng-style = "state.view === 2 && {'opacity': '1'}"
          ) mavis Containers

        ul.list.popover-list(
          ng-style = "state.view === 2 && {'opacity': '1'}"
        )
          li.list-item.popover-list-item(
            ng-repeat = "branch in mavisBranches"
          ) {{branch}}