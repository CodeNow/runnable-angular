.modal-header
  h1.modal-heading Welcome Aboard!

.grid-block.vertical.padding-sm
  p.margin-top-sm.margin-bottom-md.p.text-center.weight-light Which team’s code do&#32;
    br.visible-xxxs
    | you want to run?

  ol.grid-block.vertical.list
    li.grid-block.align-center.list-item.btn.white(
      data-event-name = "Org Selected"
      ng-click = "COMC.actions.createOrCheckDock(org.oauthName())"
      ng-repeat = "org in COMC.allAccounts.models | orderBy:'attrs.login'"
      title = "{{org.oauthName()}}"
    )
      img.grid-content.shrink(
        height = "30"
        ng-src = "{{org.gravitar()}}"
        width = "30"
      )
      span.grid-content.text-left.text-overflow {{org.oauthName()}}
      svg.grid-content.iconnables.icons-arrow-forward.shrink
        use(
          xlink:href = "#icons-arrow-down"
        )

  ol.grid-block.vertical.list
    li.grid-block.align-center.list-item.btn.white(
    ng-if = "$root.featureFlags.personalAccounts"
      data-event-name = "PA Selected"
      ng-click = "COMC.actions.createOrCheckDock(COMC.user.oauthName())"
      title = "{{COMC.user.oauthName()}}"
    )
      img.grid-content.shrink(
        height = "30"
        ng-src = "{{COMC.user.gravitar()}}"
        width = "30"
      )
      span.grid-content.text-left.text-overflow {{COMC.user.oauthName()}}
      svg.grid-content.iconnables.icons-arrow-forward.shrink
        use(
          xlink:href = "#icons-arrow-down"
        )

  ol.margin-top-sm.grid-block.vertical.list
    li.grid-block.align-center.list-item.btn.white(
      data-event-name = "Clicked to Grant Access View"
      ng-click = "goToPanel('grantAccess')"
    )
      .grid-content.shrink.icons-missing-org 🕵
      span.grid-content.text-left.text-overflow Don’t see your team?
      svg.grid-content.iconnables.icons-arrow-forward.shrink
        use(
          xlink:href = "#icons-arrow-down"
        )

footer.grid-block.align-center.vertical.padding-top-xxs.padding-bottom-md(
  ng-if = "!$root.featureFlags.personalAccounts"
)
  a.grid-block.shrink.align-center.btn-skip.text-gray.weight-light.link(
    data-event-name = "Demo: Clicked Skip"
    ng-click = "COMC.isInDemoFlow = true; goToPanel('createOrg')"
    ng-if = "$root.featureFlags.demoProject"
  ) Try a Demo
    svg.iconnables.icons-arrow-forward
      use(
        xlink:href = "#icons-arrow-down"
      )
