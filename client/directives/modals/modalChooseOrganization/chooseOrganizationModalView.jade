.modal-backdrop
  .welcome
    //- h1.grid-block.justify-center.shrink.h1.text-center.strong.padding-sm Welcome to Runnable!
    img.grid-block.shrink.img.runnabear(
      height = "130"
      src = "/build/images/runnabear-waving-3.png"
      width = "230"
    )
    .grid-block.shrink.modal-dialog.modal-orgs(
      ng-class = "{\
        'modal-lg': COS.allAccounts.length > 1,\
        'modal-sm': COS.allAccounts.length <= 1\
      }"
    )
      animated-panel-container
        animated-panel.grid-block.vertical.padding-md(
          default = "true"
          name = "orgSelection"
        )

          p.p.text-gray.fade.text-center(
            ng-class = "{'in': isActivePanel()}"
          ) Welcome! We’re so glad you made it.
            br
            | Now select the organization you want to use.

          .grid-block.vertical.list.fade(
            name = "COS.orgSelectorForm"
            ng-class = "{'in': isActivePanel()}"
            ng-click = "goToPanel('confirmOrg')"
          )
            .grid-block.align-center.list-item.btn.white(
              ng-repeat = "org in COS.allAccounts"
              title = "{{org.oauthName()}}"
            )
              img.grid-content.shrink(
                height = "30"
                ng-src = "{{org.gravitar()}}"
                width = "30"
              )
              span.grid-content.text-left.text-overflow {{org.oauthName()}}
              svg.grid-content.iconnables.icons-arrow-forward.shrink
                use(
                  xlink:href = "#icons-arrow-down"
                )

          .grid-block.vertical.list.fade(
            ng-class = "{'in': isActivePanel()}"
          )
            .grid-block.align-center.list-item.btn.white
              img.grid-content.shrink(
                height = "30"
                src = "{{org.gravitar()}}"
                width = "30"
              )
              span.grid-content.text-left.text-overflow Don’t see your organization?
              svg.grid-content.iconnables.icons-arrow-forward.shrink
                use(
                  xlink:href = "#icons-arrow-down"
                )

          .link.small.text-gray.text-center.fade(
            ng-class = "{'in': isActivePanel()}"
          ) Looking for your personal account?

        animated-panel.modal-body.grid-block.vertical.align-center(
          name = "confirmOrg"
        )
          .modal-header Got it! You’ll be using this org:
          .grid-block.align-center
            img.grid-content.shrink.fade(
              height = "60"
              ng-class = "{'in': isActivePanel()}"
              src = "https://avatars.githubusercontent.com/u/20309738?v=3"
              width = "60"
            )
            p.grid-content.p taylor-test-org
          footer.modal-footer
            button.btn.btn-md.green Confirm Organization

        animated-panel.modal-body.grid-block.vertical(
          name = "dockLoading"
        )
          .padding-md.fade(
            ng-class = "{'in': isActivePanel()}"
          )
            //- dock spinning up
            p.p.text-gray.padding-xs.text-center(
              ng-if = "!$root.featureFlags.contingencyPlan"
            ) We’re spinning up infrastructure for your org. It’ll take about&#32;
              strong.strong 10 minutes
              | . We’ll email you and update this page when it’s ready.
            .grid-block.align-center.justify-center.padding-sm(
              ng-if = "!$root.featureFlags.contingencyPlan"
            )
              .spinner-wrapper.spinner-md.spinner-gray(
                ng-include = "'spinner'"
              )
              //- ng-if = "isActivePanel()"

            p.p.text-gray.text-center.strong(
              ng-if = "$root.featureFlags.contingencyPlan"
            ) We’re running slower than normal due to high demand.
            p.p.text-gray.text-center.padding-xs(
              ng-if = "$root.featureFlags.contingencyPlan"
            ) We’ll email you at&#32;
              span {{ COS.user.attrs.email }}
              | &#32;as soon we’re done spinning up your infrastructure. Thanks for your patience.
        animated-panel.modal-body.grid-block.vertical(
          name = "dockLoaded"
        )
          .padding-md.fade(
            ng-class = "{'in': isActivePanel()}"
          )
            //- dock spun up
            p.p.text-gray.padding-xs.text-center Thanks for waiting, we‘re ready for you now.

          //- show this button when done waiting
          footer.grid-block.vertical.modal-footer.fade(
            ng-class = "{'in': isActivePanel()}"
          )
            button.btn.btn-md.green(
              data-event-name = "Nav to Runnable (after infrastructure wait)"
              ng-click = "actions.selectAccount(COS.selectedOrgName)"
            ) Go to Runnable
    .grid-block.justify-center.modal-outer-footer(
      grace-period-footer
      hide-choose-org = "true"
    )

    .grid-block.shrink.modal-dialog.modal-sm
      .modal-header
        svg.iconnables.icons-arrow-backward.fade(
          ng-class = "{'in': isActivePanel()}"
        )
          use(
            xlink:href = "#icons-arrow-down"
          )
        h1.modal-heading Ready to Go?
      .grid-block.vertical.align-center.justify-center.padding-xl
        img.grid-content.shrink.fade(
          height = "66"
          ng-class = "{'in': isActivePanel()}"
          src = "https://avatars.githubusercontent.com/u/20309738?v=3"
          width = "66"
        )
        h3.h3.grid-content.shrink.text-gray taylor-test-org
      footer.grid-block.modal-footer
        button.grid-block.justify-center.btn.btn-md.green Confirm Organization
