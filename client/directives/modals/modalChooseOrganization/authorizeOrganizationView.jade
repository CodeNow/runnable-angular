.modal-header
  svg.iconnables.icons-arrow-backward(
    ng-click = "goToPanel('createOrg', 'back')"
  )
    use(
      xlink:href = "#icons-arrow-down"
    )
  h1.modal-heading Add your new organization.

.grid-block.align-center.vertical.padding-lg
  p.p.weight-light Now add your new org to Runnable by granting access on GitHub. Then we’ll fork a demo repo into it to get you started.

  //- hide after clicked
  a.grid-block.align-center.justify-center.shrink.btn.btn-md.green.margin-top-md(
    data-event-name = "Demo: Clicked Grant Access"
    ng-click = "COMC.grantAccess('selectNewOrg')"
    ng-if = "!$root.isLoading.grantAccess"
  )
    svg.iconnables.margin-right-xxs
      use(
        xlink:href = "#icons-octicons-github"
      )
    | Grant Access
    svg.iconnables.icons-link-external.text-white
      use(
        xlink:href = "#icons-link-external-alt"
      )

  //- show after user clicks "grant access"
  .well.gray.align-center.small.padding-sm.padding-top-xs.margin-top-md(
    ng-if = "$root.isLoading.grantAccess"
  )
    svg.iconnables.icons-github-listening
      use(
        xlink:href = "#icons-github-listening"
      )
    | We’ll update this page once you grant&#32;
    br.hidden-xxs
    | access to an org on GitHub.
    br
    a.btn.btn-xs.green.margin-top-xs(
      ng-click = "COMC.grantAccess('selectNewOrg')"
    )
      svg.iconnables.icons-github
        use(
          xlink:href = "#icons-octicons-github"
        )
      | Grant Access…
