.modal-backdrop.in
  .modal-dialog.modal-launch-branches
    header.modal-header
      h1.modal-heading Launch Branches
      svg.iconnables.icons-close(
        ng-click = "defaultActions.cancel()"
      )
        use(
          xlink:href = "#icons-close"
        )
    section.modal-body This will automatically launch containers for branches of api, starting with the 8 active branches you have now.
    section.modal-footer
      button.btn.btn-block.green Launch Branches