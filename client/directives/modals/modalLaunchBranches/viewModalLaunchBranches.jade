.modal-backdrop.in
  .modal-dialog.modal-launch-branches
    header.modal-header
      h1.modal-heading Enable api Branches
      svg.iconnables.icons-close(
        ng-click = "defaultActions.cancel()"
      )
        use(
          xlink:href = "#icons-close"
        )
    section.modal-body
      span.span This will launch containers for active branches of&#32;
      strong.strong api.&#32;
      | From now on, a new container will launch when you create a branch.
      .instances-list
        .list
          .list-item.list-item-header
            span.span.text-overflow api
            svg.iconnables.icons-advanced
              use(
                xlink:href = "#icons-advanced"
              )
          .list-item
            a.list-item-container
              .icons-status.green
              .text-overflow master
          .list-item
            a.list-item-container
              .icons-status.green
              .text-overflow my-branch
          .list-item
            .list-item-container
              button.btn.btn-sm.green Launch Branches