section.label-description
  .label-col Private Registry
  .small.text-gray If you push your images to a private registry, you can safely store your credientials to use those images on Runnable.&#32;
    a.link(
      href = "//runnable.com/docs"
      target = "_blank"
    ) Learn more
    | .

form.grid-block.vertical.well.gray.ignore-margin
  label.padding-sm.label
    .padding-xxs.small.label-sm Registry URL
    input.input.input-md.input-validate(
      name = "data.url"
      ng-disabled = "state.saving"
      ng-required
      placeholder = "https://index.docker.io/v1/"
      spellcheck = "false"
    )
    small.grid-block.padding-xxs.small Add the URL for the registry you use. This could be from Docker Hub, AWS, Google Cloud, etc..&#32;
      a.link(
        href = "//runnable.com/docs"
        target = "_blank"
      ) Learn more
      | .
  label.padding-sm.label
    .padding-xxs.small.label-sm Username
    input.input.input-md.input-validate(
      name = "data.username"
      ng-disabled = "state.saving"
      ng-required
      spellcheck = "false"
    )
  label.padding-sm.label
    .padding-xxs.small.label-sm Password
    input.input.input-md.input-validate(
      name = "data.password"
      ng-disabled = "state.saving"
      ng-required
      spellcheck = "false"
      type = "password"
    )
  footer.margin-top-sm.padding-sm.grid-block.justify-right.footer
    //- ng-click = "state.saving = true"
    button.grid-block.shrink.align-center.btn.btn-sm.green(
      ng-disabled = "state.saving"
      ng-init = "state.saving = null"
    )
      .spinner-wrapper.spinner-sm.spinner-white(
        ng-if = "state.saving"
        ng-include = "'spinner'"
      )
      span Save
