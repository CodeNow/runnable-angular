//- changing plans
.padding-md
  .clearfix
    .label-col.full-width.text-center Configurations Used
  .meter(
    ng-class = "getMeterClass()"
  )
    .current-plan
    .ticks
      .tick
      .tick(
        ng-class = "{'expand': preview === 'starter'}"
      )
      .tick
      .tick
      .tick
      .tick
      .tick(
        ng-class = "{'expand': preview === 'standard'}"
      )
      .tick
      .tick
      .tick
      .tick
      .tick
      .tick
      .tick

  .grid-block.align-center
    .grid-block.vertical.align-center.card.card-plan.disabled.padding-sm.text-center(
      ng-class = "{'active': PSFC.plan.id === 'starter'}"
      ng-mouseenter = "preview = 'starter'"
      ng-mouseleave = "preview = ''"
    )
      h3.grid-content.shrink.h3 Starter
      .grid-block.shrink.vertical.price
        .grid-block.strike(
          ng-if = "PSFC.discounted"
        ) ${{plans.starter.costPerUser}}
        .grid-block.justify-center.h1(
          ng-class = "{'discounted': PSFC.discounted}"
        )
          sup.sup $
          | {{PSFC.calculatePlanAmount('starter')| money}}
        small.grid-block.vertical.small.text-gray per user
          br
          | per month
      .grid-content.shrink.small Up to&#32;
        strong.strong 2
        | &#32; configurations.

    .grid-block.vertical.align-center.card.card-plan.disabled.padding-sm.text-center(
      ng-class = "{'active': PSFC.plan.id === 'standard'}"
      ng-mouseenter = "preview = 'standard'"
      ng-mouseleave = "preview = ''"
    )
      h3.grid-content.shrink.h3 Standard
      .grid-block.shrink.vertical.price
        .grid-block.strike(
          ng-if = "PSFC.discounted"
        ) ${{plans.standard.costPerUser}}
        .grid-block.justify-center.h1(
          ng-class = "{'discounted': PSFC.discounted}"
        )
          sup.sup $
          | {{PSFC.calculatePlanAmount('standard') | money}}
        small.grid-block.vertical.small.text-gray per user
          br
          | per month
      .grid-content.shrink.small Up to&#32;
        strong.strong 7
        | &#32; configurations.

    .grid-block.vertical.align-center.card.card-plan.disabled.padding-sm.text-center(
      ng-class = "{'active': PSFC.plan.id === 'plus'}"
      ng-mouseenter = "preview = 'plus'"
      ng-mouseleave = "preview = ''"
    )
      h3.grid-content.shrink.h3 Plus
      .grid-block.shrink.vertical.price
        .grid-block.strike(
          ng-if = "PSFC.discounted"
        ) ${{plans.plus.costPerUser}}
        .grid-block.justify-center.h1(
          ng-class = "{'discounted': PSFC.discounted}"
        )
          sup.sup $
          | {{PSFC.calculatePlanAmount('plus')| money}}
        small.grid-block.vertical.small.text-gray per user
          br
          | per month
      .grid-content.shrink.small Up to&#32;
        strong.strong 15
        | &#32; configurations.

  label.grid-block.align-center.padding-xs.well.well-plan.well-summary.disabled(
    ng-if = "PSFC.discounted"
    ng-include = "'discountView'"
    ng-init = "state.hasDuration = (PSFC.activeAccount.isInActivePeriod()) ? 'true' : ''"
  )

  p.grid-content.p.text-gray.text-center.padding-sm(
    payment-summary
    ng-init = "\
      state.hasPaid = false;\
      state.showNext = true;\
    "
  )

  .grid-block.justify-center
    button.grid-block.shrink.btn.btn-md.green(
      ng-click = "\
        SEMC.showFooter = false;\
        goToPanel('changePaymentForm');\
      "
      ng-if = "PSFC.activeAccount.isInTrial()"
      type = "button"
    ) Add Payment Method

footer.modal-footer.clearfix(
  ng-if = "!PSFC.activeAccount.isInTrial()"
)
  button.btn.btn-md.gray.btn-cancel.float-left(
    ng-click = "\
      SEMC.showFooter = true;\
      goToPanel('billingForm', 'back');\
    "
  ) Back
