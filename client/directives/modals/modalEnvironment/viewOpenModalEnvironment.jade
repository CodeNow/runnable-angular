.modal-backdrop.in(
  ng-click = "$event.stopPropagation()"
  ng-if = "in"
)
   .modal-dialog.modal-sidebar.modal-environment(
     actions = "actions"
     current-model = "currentModel"
     data = "data"
     default-actions = "defaultActions"
     modal-environment
     ng-class = "{'show-aside': data.toggleSidebar}"
     state-model = "stateModel"
   )