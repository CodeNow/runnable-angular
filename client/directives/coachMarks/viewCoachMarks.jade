//- *
//- coach marks
//- *
//- box name mark
.guide.guide-mark(
  ng-click = "$event.stopPropagation()"
  ng-model = "coachMarkData"
  ng-class = "{in: !coachMarkData.hasBeenViewed}"
  ng-show = "!coachMarkData.hasBeenViewed"
  toggle-pop-over
  toggle-pop-over-model = "coachMarkData.show"
  ng-style = "coachMarkData.getStyle()"
)
  pop-over(
    data = "coachMarkData"
    popover-options = "{\"left\":-126,\"top\":33}"
    template = "{{template}}"
  )