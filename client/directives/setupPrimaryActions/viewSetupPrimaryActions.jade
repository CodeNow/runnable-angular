.primary-actions
  //- only validates name
  button.btn(
    ng-class = "{ \
      orange: !isNameValid, \
      green: isNameValid \
    }"
    ng-click = "buildAndAttach()"
    ng-disabled = "!data.build || loading || !isNameValid || !isDockerFileValid"
  ) Create Server
