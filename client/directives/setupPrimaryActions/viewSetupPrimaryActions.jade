.primary-actions
  //- remove .orange, add .green when validation passes
  button.btn(
    ng-class = "{ \
      orange: (!valid || !name), \
      green: (valid && name) \
    }"
    ng-click = "buildAndAttach()"
    ng-disabled = "!data.build || loading || !valid || !name"
  ) Create Box
