.popover.menu.right.popover-container-menu(
  ng-class = "{in: active}"
  ng-init = "\
    isolatedContainters = [\
      {name: 'api'},\
      {name: 'mavis'},\
      {name: 'runnable-angular'}\
    ];\
  "
  ng-style = "popoverStyle.getStyle()"
  style = "transform-origin: 0 50%"
)
  .arrow.white
  //- menu items:

    - for a non-isolated branch:
      - Setup Isolation
      - (divider)
      - Hide

    - for an isolated branch:
      - Configure Container
      - (divider)
      - Add Container to Isolation
      - Disable Isolation
      - (divider)
      - Hide

    - for an isolated branch's containers:
      - Configure Container
      - (divider)
      - Delete Container from Isolation

  .popover-content
    ul.list.popover-list
      //- li.list-item.popover-list-item
      //-   svg.iconnables
      //-     use(
      //-       xlink:href = "#icons-advanced"
      //-     )
      //-   | Configure Container
      //- li.divider
      li.list-item.popover-list-item.multi-line.disabled
        p.p.text-center Isolated branches have their own copy of&#32;
          strong.strong all containers in your stack&#32;
          |, so changes to those containers do not affect staging branches.
          .btn.btn-sm.btn-block.green(
            modal
            modal-state-model = "{modalFlip: true}"
            modal-template = "viewModalIsolation"
          ) Setup Isolation

        //- svg.iconnables
        //-   use(
        //-     xlink:href = "#icons-isolation"
        //-   )
        //- | Setup Isolation…

      //- li.divider
      //- li.list-item.popover-list-item
      //-   svg.iconnables
      //-     use(
      //-       xlink:href = "#icons-isolation-enable"
      //-     )
      //-   | Add Container to Isolation
      //- li.list-item.popover-list-item
      //-   svg.iconnables
      //-     use(
      //-       xlink:href = "#icons-delete"
      //-     )
      //-   | Disable Isolation
      //- li.list-item.popover-list-item
      //-   svg.iconnables
      //-     use(
      //-       xlink:href = "#icons-isolation-disable"
      //-     )
      //-   | Delete Container from Isolation

      //- navListFilter
      li.divider(
        ng-if = "$root.featureFlags.navListFilter"
      )
      li.list-item.popover-list-item(
        ng-if = "$root.featureFlags.navListFilter"
      )
        svg.iconnables
          use(
            xlink:href = "#icons-hide"
          )
        | Hide
