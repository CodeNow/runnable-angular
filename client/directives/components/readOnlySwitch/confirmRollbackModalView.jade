.modal-backdrop.in
  .modal-dialog.modal-alert
    header.modal-body
      svg.iconnables.icons-close(
        ng-click = "CMC.actions.cancel()"
      )
        use(
          xlink:href = "#icons-close"
        )
      p.p.strong.popover-title Disable editing and restore past configuration?
      p.p Disabling will restore your container’s configuration from when editing was initially enabled {{CMC.instance.attrs.lastBuiltSimpleContextVersion.created | date : 'MMM d, y h:mm a'}}. You can’t undo this action.
    footer.modal-footer.clearfix
      button.btn.red.float-right(
        ng-click = "CMC.actions.confirm()"
      ) Disable and Restore
      button.btn.white.btn-cancel(
        ng-click = "CMC.actions.cancel()"
      ) Cancel