.grid-block.shrink.btn.btn-container-menu(
  ng-class = "{\
    'active': BTPBC.popoverActive,\
    'orange': BTPBC.sha.length > 0\
  }"
  ng-click = "BTPBC.popoverOpen = true"
  ng-if = "$root.featureFlags.composeHistory"
  pop-over
  pop-over-template = "branchTestSelectorPopoverView"
  pop-over-data = "BTPBC.popoverData"
  pop-over-options = "{\"verticallyCentered\":true,\"right\":-372,\"pinToViewPort\": true}"
  pop-over-active = "BTPBC.popoverActive"
)
  span.span {{BTPBC.sha | limitTo:7}}
  svg.iconnables
    use(
      xlink:href = "{{$root.featureFlags.multipleWebhooks ? '#icons-gear': '#icons-history'}}"
    )
