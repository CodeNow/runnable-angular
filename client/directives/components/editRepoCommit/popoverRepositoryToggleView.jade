//- *************************
//- deprecated file, delete with $root.featureFlags.additionalRepositories

//- .popover-repository-toggle
.popover.right.popover-repository-toggle(
  ng-class = "{'in': active}"
  ng-style = "popoverStyle.getStyle()"
)
  .popover-content
    //- spinner while commits are loading
    .spinner-wrapper.spinner-md.in(
      ng-if = "!data.branch.commits.models.length"
      ng-include = "'spinner'"
    )
    //- commits
    ul.list.popover-list
      li.list-item.popover-list-item(
        ng-class = "{ \
          active: (commit.attrs.sha === data.commit.attrs.sha) \
        }"
        ng-click = "actions.selectCommit(commit.attrs.sha)"
        ng-repeat = "commit in data.branch.commits.models"
      )
        .toggle-group-title.text-overflow(
          ng-attr-title = "{{ ::commit.attrs.commit.message }}"
        ) {{ ::commit.attrs.commit.message }}
        .toggle-group-detail
          small.small.text-overflow(
            ng-attr-title = "{{ ::commit.attrs.author.login }} authored {{ commit.attrs.commit.author.date | timeFrom }}"
          ) {{ ::commit.attrs.author.login }} authored {{ commit.attrs.commit.author.date | timeFrom }}
          //- we don't want to close the popover because they are clicking on an external link
          a.link.monospace(
            ng-href = "{{ commit.attrs.html_url }}"
            ng-click = "$event.stopPropagation()"
            target = "_blank"
          ) {{ ::commit.attrs.sha | limitTo:7 }}

//- end deprecated file, delete with $root.featureFlags.additionalRepositories
//- *************************
