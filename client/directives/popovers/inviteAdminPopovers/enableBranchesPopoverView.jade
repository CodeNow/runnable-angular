.popover.popover-tooltip.right(
  ng-class = "{'in': active}"
  ng-style = "popoverStyle.getStyle()"
)
  .arrow

  //- if default container is not running
  small.small(
    ng-if = "$root.featureFlags.webhooks && !$root.featureFlags.webhooksContainerRunning"
  ) You can add branches once your default container is running.

  //- if default container is running but there's no admin present
  small.small(
    ng-if = "$root.featureFlags.webhooks && $root.featureFlags.webhooksContainerRunning && !$root.featureFlags.webhooksAdminPresent"
  ) You’re one step away; we just need permission from a GitHub admin.
  button.btn.btn-xs(
    ng-click = "EBPC.openInviteAdminModal(data); POC.closePopover();"
    ng-if = "$root.featureFlags.webhooks && $root.featureFlags.webhooksContainerRunning && !$root.featureFlags.webhooksAdminPresent"
  ) Invite an Admin…