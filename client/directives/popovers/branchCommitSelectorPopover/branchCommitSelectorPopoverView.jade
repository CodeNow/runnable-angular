.popover.right.popover-branch-menu(
  ng-class = "{'in': active}"
  ng-style = "popoverStyle.getStyle()"
)
  .arrow.white(
    ng-style = "popoverStyle.getArrowStyle()"
  )

  section.popover-content.list-actions.list-commits(
    branch-commit-list
    instance = "data.instance"
  )
  section.section-secondary.padding-xs(
    ng-if = "$root.featureFlags.multipleWebhooks"
  )
    .padding-left-xxs.padding-right-xxs.margin-bottom-xxs.text-gray.small Deleting removes environments created from the&#32;
      //- should link to the Compose file in their default branch
      a.link(
        href = ""
        target = "_blank"
      ) Compose file
      | &#32;in your default branch from Runnable.

    ul
      li.grid-block.align-center.list-item.popover-list-item
        svg.iconnables.icons-delete
          use(
            xlink:href = "#icons-delete"
          )
        | Delete Environments…