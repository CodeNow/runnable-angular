.popover.menu.bottom.popover-add-tab(
  ng-class = "{in: active}"
  ng-style = "popoverStyle.getStyle()"
)
  .arrow.white
  .popover-content
    ul.list.popover-list
      li.list-item.popover-list-item.small Open Tab
      li.list-item.popover-list-item(
        ng-click = "actions.addTerminal(); closePopover()"
        ng-if = "data.options.term"
      )
        svg.iconnables
          use(
            xlink:href = "#icons-start-cmd"
          )
        | Terminal
      li.list-item.popover-list-item(
        ng-click = "actions.addBuildStream(); closePopover()"
        ng-if = "data.options.build"
      )
        svg.iconnables
          use(
            xlink:href = "#icons-build-logs"
          )
        | Build Logs
      li.list-item.popover-list-item(
        ng-click = "actions.addLogs(); closePopover()"
        ng-if = "data.options.server"
      )
        svg.iconnables
          use(
            xlink:href = "#icons-cmd-logs"
          )
        | CMD Logs