.popover.popover-admin-needed.right(
  ng-class = "{'in': active}"
  ng-style = "popoverStyle.getStyle()"
)
  .arrow

  //- if popover was triggered through containers list
  | To add branches for this repository, we need permission from one of its GitHub admins.

  //- if popover was triggered through Auto Deploy
  //- | To enable Auto Deploy for this repository, we need permission from one of its GitHub admins.

  button.btn.btn-xs(
    modal
    modal-template = "inviteAdminModalView"
  ) Invite an Admin…