//- new alerts
.notification.notification-sm.environment-notification.js-animate(
  ng-class = "{\
    'green': alert.type === 'success',\
    'red': alert.type === 'deleted'\
  }"
  ng-if = "$root.featureFlags.inviteFlows && alert"
)
  svg.iconnables.icons-check.float-left
    use(
      xlink:href = "#icons-check"
    )
  //- omg
  span(
    ng-if = "alert.text === 'Your new container is building.'"
  ) Container Created
  span(
    ng-if = "alert.text === 'Container updated successfully.'"
  ) Changes Saved
  span(
    ng-if = "deleted"
  ) Container Deleted
    a.link Undo

//- old alerts
.help-container.fixed.bottom(
  ng-if = "!$root.featureFlags.inviteFlows"
)
  .help(
    ng-if = "alert"
  )
    svg.iconnables.icons-check(
      ng-if = "alert.type === 'success'"
    )
      use(
        xlink:href = "#icons-check"
      )
    | {{alert.text}}

//- environment page
.environment-wrapper
  header.environment-header.clearfix(
    ng-include = "'viewEnvironmentHeader'"
    ng-init = "state.helpButton = {active: false}"
  )

  .environment-body.clearfix
    .help-container(
      ng-class = "{\
        'fixed': helpUndock,\
        'top': helpUndock\
      }"
      ng-if = "helpCards.getActiveCard()"
    )
      .help(
        ng-bind-html = "helpCards.getActiveCard().helpPopover.newContainer"
        ng-if = "helpCards.getActiveCard().targets.newContainer && state.newServerButton.active"
      )
      .help(
        ng-bind-html = "helpCards.getActiveCard().helpTop"
        ng-if = "!(helpCards.getActiveCard().targets.newContainer && state.newServerButton.active)"
      )
    .help-padding.no-touching(
      ng-bind-html = "helpCards.getActiveCard().helpTop"
      ng-if = "helpCards.getActiveCard()"
    )
    .card-grid.clearfix(
      ng-include = "'viewCardGrid'"
    )

  footer.environment-footer.small.text-center(
    ng-if = "$root.featureFlags.inviteFlows"
  )
    a.link(
      internal-modal-helper = "inviteModalView"
    )
      svg.iconnables.icons-team-invite
        use(
          xlink:href = "#icons-team-invite"
        )
      | Invite a teammate
    | &#32;to help you set up your stack.