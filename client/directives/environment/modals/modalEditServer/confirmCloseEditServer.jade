.modal-backdrop.in
  .modal-dialog.modal-sm.modal-alert(
    ng-if = "!$root.featureFlags.rebuildFlow"
  )
    header.modal-body
      p.p.strong Are you sure you want to discard your changes and exit?
    footer.modal-footer.clearfix
      button.btn.btn-sm.gray.float-left(
        ng-click = "CMC.actions.cancel()"
      ) Cancel
      button.btn.btn-sm.red.float-right(
        ng-click = "CMC.actions.confirm()"
      ) Discard and Exit

  .modal-dialog.modal-sm.modal-alert(
    ng-if = "$root.featureFlags.rebuildFlow"
  )
    header.modal-body
      p.p.strong You’ve made changes since your last build. Do you want to save your changes before closing?
    footer.modal-footer.clearfix
      button.btn.btn-sm.gray.float-left(
        ng-click = "CMC.actions.cancel()"
      ) Go Back
      button.btn.btn-sm.gray.float-right Save & Build
      button.btn.btn-sm.red.float-right Discard
