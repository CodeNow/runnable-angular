.label.clearfix
  .label-col Language / Framework

  .input-col
    fancy-select(
      value = "currentModel.selectedStack"
      placeholder = "Select language/framework"
    )
      li.list-item.popover-list-item.small(
        ng-if = "$parent.currentModel.repo.stackAnalysis.languageFramework"
      ) Suggested Language/Framework
      fancy-option(
        value = "framework"
        ng-repeat = "framework in $parent.data.stacks | filter: { key: $parent.currentModel.repo.stackAnalysis.languageFramework }"
      )
        svg.iconnables.icons-server
          use(
            xlink:href = "#icons-server"
          )
        | {{$parent.framework.name}}

      li.divider(
        ng-if = "$parent.currentModel.repo.stackAnalysis.languageFramework"
      )
      fancy-option(
        value = "framework"
        ng-repeat = "framework in $parent.data.stacks | filter: { key: '!' + $parent.currentModel.repo.stackAnalysis.languageFramework } | orderBy:'name'"
      )
        svg.iconnables.icons-server
          use(
            xlink:href = "#icons-server"
          )
        | {{$parent.framework.name}}
    fancy-select(
      value = "$parent.currentModel.selectedStack.selectedVersion"
      placeholder = "Select Version"
    )
      li.list-item.popover-list-item.small(
        ng-if = "$parent.currentModel.selectedStack.suggestedVersion"
      ) Suggested {{$parent.$parent.currentModel.selectedStack.name}} Version
      fancy-option(
        value = "version"
        ng-repeat = "version in $parent.currentModel.selectedStack.versions | filter: $parent.currentModel.selectedStack.suggestedVersion"
      ) v{{$parent.version}}
      li.divider(
        ng-if = "$parent.currentModel.selectedStack.suggestedVersion"
      )
      fancy-option(
        value = "version"
        ng-repeat = "version in $parent.currentModel.selectedStack.versions | filter: '!'+ $parent.currentModel.selectedStack.suggestedVersion"
      ) v{{$parent.version}}
    .label(
      ng-repeat = "depStack in currentModel.selectedStack.dependencies"
    )
      .small {{depStack.name}}
      fancy-select(
        value = "depStack.selectedVersion"
        placeholder = "Select Version"
      )
        li.list-item.popover-list-item.small(
          ng-if = "$parent.depStack.suggestedVersion"
        ) Suggested {{$parent.$parent.depStack.name}} Version
        fancy-option(
          value = "version"
          ng-repeat = "version in $parent.depStack.versions | filter: $parent.depStack.suggestedVersion"
        ) v{{$parent.version}}
        li.divider(
          ng-if = "$parent.depStack.suggestedVersion"
        )
        fancy-option(
          value = "version"
          ng-repeat = "version in $parent.depStack.versions | filter: '!'+ $parent.depStack.suggestedVersion"
        ) v{{$parent.version}}