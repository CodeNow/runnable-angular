//- do not show if no preview
.clearfix(
  ng-if = "true"
)
  label.label.label-jump
    small.small Jump to
    select.select
      optgroup(
        label = "Filename rule"
      )
        option build/index.html
      optgroup(
        label = "String rule"
      )
        option build/index2.html
        option build/assets/img.img

.table.table-diff(
  ng-include = "'viewRuleFilename'"
)

.table.table-diff(
  ng-include = "'viewRuleString'"
)

.table.table-diff(
  ng-include = "'viewRuleString'"
)

//- if no preview to show
p.p.empty(
  ng-if = ""
) There's nothing to see here