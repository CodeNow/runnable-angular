.clearfix
  .label-col SSH Keys
  .input-col
    button.btn.btn-xs.white.float-right(
      pop-over
      pop-over-template = "viewPopoverSshKey"
    ) Add Key
    label.btn.btn-xs.white.float-right(
      for = "input-upload"
    ) Upload File…
      input#input-upload(
        type = "file"
      )

.table.clearfix
  .tr.multi-line.clearfix
    .cell.text-overflow Key Title
      small.small Added on May 5th, 2000
    .cell.cell-action
      button.btn.btn-xxs.red Delete

//- if no keys
//- p.p.empty Add an SSH Key for reasons.