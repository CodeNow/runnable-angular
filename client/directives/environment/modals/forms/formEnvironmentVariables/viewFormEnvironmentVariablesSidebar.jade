.btn-group(
  ng-if = "$root.featureFlags.hostnameNotifications"
  ng-init = "data.viewHidden = true"
)
  .btn.btn-xs.purple(
    ng-class = "{active: !data.viewHidden}"
    ng-click = "data.viewHidden = false"
  ) Hostnames
  .btn.btn-xs.purple(
    ng-class = "{active: data.viewHidden}"
    ng-click = "data.viewHidden = true"
  ) Hidden

//- empty state
p.p.small.empty(
  ng-if = "data.viewHidden && $root.featureFlags.hostnameNotifications"
) You haven’t hidden any hostnames…yet.
//- p.p.small.empty Add hostnames to your environment variables file to see them here.

//- clicks on .well takes user to line number
ul.list
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 1
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 2
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 200
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 200
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 200
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 200
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 200
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) api-staging-CodeNow.runnableapp.com
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide
  li.well.small(
    ng-if = "!data.viewHidden"
  ) Jump to Line 200
    .small.monospace.text-overflow(
      title = "api-staging-CodeNow.runnableapp.com"
    ) whoreisland
    button.btn.btn-xxs.btn-ignore(
      ng-click = "$event.stopPropagation()"
      ng-if = "$root.featureFlags.hostnameNotifications"
    ) Hide

//-
//- previous popover:
//-
//- .popover.in.menu.top(
//-   ng-class = "{in: active}"
//-   ng-style = "popoverStyle.getStyle()"
//- )
//-   .arrow.white
//-   .popover-content
//-     ul.popover-list
//-       li.list-item.popover-list-item.small(
//-         ng-if = "data.servers.length"
//-       ) Linked containers (by line)
//-       li.list-item.popover-list-item.monospace.text-overflow(
//-         ng-repeat = "link in data.servers"
//-       )
//-         button.btn.btn-xxs.gray(
//-           ng-attr-title = "Go to line {{ link.line }}"
//-         ) L {{ link.line }}
//-         | {{ link.url }}
//-       li.list-item.popover-list-item.divider(
//-         ng-if = "data.others.length && data.servers.length"
//-       )
//-       li.list-item.popover-list-item.small(
//-         ng-if = "data.others.length"
//-       ) Other links
//-       li.list-item.popover-list-item.monospace.text-overflow(
//-         ng-repeat = "link in data.others"
//-       )
//-         button.btn.btn-xxs.gray(
//-         ng-attr-title = "Go to line {{ link.line }}"
//-         ) L {{ link.line }}
//-         | {{ link.url }}