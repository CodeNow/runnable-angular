.clearfix
  .label-col Environment Variables
  //- .input-col
    button.btn.btn-xs.green(
      pop-over
      pop-over-actions = "popoverEnvironmentVariable.actions"
      pop-over-active = "popoverEnvironmentVariable.data.show"
      pop-over-data = "popoverEnvironmentVariable.data"
      pop-over-options = "{\"top\":-72}"
      pop-over-template = "viewPopoverEnvironmentVariable"
    ) New Linking Variable

//- table of current environment variables
pre.pre(
  ui-ace
)
.table-actions(
  ng-if = "!data.insertHostName"
)
  button.btn.btn-xs.gray(
    ng-click = "data.insertHostName = true"
  ) Insert host name
.table-actions(
  ng-if = "data.insertHostName"
)
  .label.label-row.clearfix
    input.input.input-sm.monospace.text-overflow(
      placeholder = "Protocol"
    )
    button.btn.btn-sm.monospace.text-overflow api-codenow.runnableapp.com
      svg.iconnables.icons-arrow-down
        use(
          xlink:href = "#icons-arrow-down"
        )
    input.input.input-sm.monospace.text-overflow(
      placeholder = "Port"
    )
  .table-footer
    button.btn.btn-xs.gray(
    ng-click = "data.insertHostName = false"
  ) Cancel
    button.btn.btn-xs.green Insert host name


small.small 👈🅱🏊📥👩👦👋💇🎦🎉 #{''}
  a.link(
    pop-over
    pop-over-actions = "popoverHostNames.actions"
    pop-over-active = "popoverHostNames.data.show"
    pop-over-data = "popoverHostNames.data"
    pop-over-options = "{\"left\":0,\"top\":39}"
    pop-over-template = "viewPopoverHostNames"
  ) More about emoji variables