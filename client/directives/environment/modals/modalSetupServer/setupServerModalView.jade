.modal-dialog
    //- if no environment
  .modal-server-select(
    ng-if = "!state.repo"
    ng-include = "'viewModalRepositorySelect'"
  )
  .modal-server-select(
    ng-if = "state.repo"
    ng-include = "'viewModalVerifyServer'"
  )