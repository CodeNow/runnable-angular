.popover.menu.bottom.popover-new-server(
  ng-class = "{in: active}"
  ng-style = "popoverStyle.getStyle()"
)
  .arrow.white
  .popover-content
    ul.popover-list
      li.list-item.popover-list-item.small Create container from…
      li.list-item.popover-list-item(
        modal
        modal-actions = "actions"
        modal-data = "data"
        modal-template = "setupServerModalOpen"
      )
        svg.iconnables
          use(
            xlink:href = "#icons-new-repository-server"
          )
        | Repository
      li.list-item.popover-list-item.multi-line(
        modal
        modal-actions = "actions"
        modal-data = "data"
        modal-template = "setupTemplateModalOpen"
        ng-class = "{ hint: data.helpCards.getActiveCard().targets.newContainer }"
      )
        svg.iconnables
          use(
            xlink:href = "#icons-server-new"
          )
        | Non-repository
        small.small Create container from templates like MySQL, Redis, etc.
