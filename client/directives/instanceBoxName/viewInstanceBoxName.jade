.server-name
  span.sever-name(
    ng-if = "instance.getRepoName()"
  ) {{ instance.getRepoName() }}
  span.sever-name(
    ng-if = "!instance.getRepoName()"
  ) {{ instance.getDisplayName() }}
  span.branch-name(
    ng-if = "instance.getBranchName()"
  ) /{{ instance.getBranchName() }}

  status-icon(
    instance = "instance"
  )
input.server-url(
  coach-mark-style = "{\"left\": -13, \"top\": 33}"
  coach-mark-template = "viewCoachMarksBoxNamePopover"
  coach-mark-type = "editButton"
  ng-if = "instance.attrs.name"
  readonly
  select-all
  show-coach-marks
  value = "{{ instance.getContainerHost() }}"
  template = "viewCoachMarksBoxNamePopover"
)
//- sets width
span.server-name-ghost.no-touching {{ instance.getContainerHost() }}
a.container-link-external(
  target = "_blank"
  ng-href = "http://{{ instance.getContainerHost() }}"
)
  svg.iconnables.icons-link-external
    use(
      xlink:href = "#icons-link-external"
    )